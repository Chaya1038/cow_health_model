<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aadhya.herdhealth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <package android:name="com.google.ar.core" />
        <package android:name="com.google.android.googlequicksearchbox" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HerdHealth"
        android:usesCleartextTraffic="true"
        tools:replace="android:label,android:icon,android:theme,android:allowBackup"
        tools:targetApi="31">

        <!-- ✅ Firebase Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Splash Screen (Launcher Activity) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ MainActivity added -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Main Activities -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".UserSelectionActivity" android:exported="false" />
        <activity android:name=".CowListActivity" android:exported="false" />
        <activity android:name=".CowDetailsActivity"
            android:exported="false"
            android:parentActivityName=".CowListActivity" />
        <activity android:name=".ARActivity" android:exported="false" />

        <!-- Veterinary Activities -->
        <activity android:name=".VetDashboardActivity" android:exported="false" />
        <activity android:name=".VetCowDetailsActivity" android:exported="false" />

    </application>

</manifest>
